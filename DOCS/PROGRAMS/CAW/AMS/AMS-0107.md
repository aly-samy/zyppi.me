# AMS-0107 — Configure GitHub Actions CI Skeleton

**Implements:** IT-0107
**Milestone:** M01 — Repository Foundation
**Size:** S
**Depends On:** IT-0106 ☑ Complete
**Status:** ☐ Planned

---

## 1. Load

Load only the following governing artifacts:

- CEngS-000
- CEngS-001
- CEngS-002
- CEngS-003
- CAW-000
- CAW-004
- CAW-011

---

## 2. Objective

Create the first GitHub Actions CI workflow for the Zyppi monorepo.

The workflow must be a **thin, deterministic, least-privilege executor** of the repository’s existing validation commands. It must not become a second build system, a shadow configuration layer, or a source of application behavior.

The workflow must establish:

1. Explicit and repository-owned toolchain authority.
2. Frozen dependency installation.
3. Minimal workflow permissions.
4. A stable baseline execution environment.
5. Mechanical enforcement of formatting, linting, TypeScript project references, and tests.

---

## 3. Background

M01 has already established:

- pnpm monorepo structure;
- TypeScript project references;
- ESLint v9 Flat Config;
- Prettier;
- Vitest with isolated mocks and serial file execution.

This task makes the existing repository checks enforceable through GitHub Actions.

At the current repository stage, the authoritative TypeScript validation command is:

```bash
pnpm exec tsc -b
```

Do **not** use or repair `pnpm -r build` merely to make CI pass.

Workspace packages that currently have no independent build behavior must **not** receive dummy, empty, or placeholder `build` scripts.

---

# 4. Constitutional Execution Rules

## 4.1 Single Toolchain Authority

The root `package.json` is the authoritative repository-owned source of truth for the Node.js and pnpm versions used by CI.

The root `package.json` must explicitly contain:

```json
{
  "engines": {
    "node": "<exact-supported-node-version>"
  },
  "packageManager": "pnpm@<exact-pnpm-version>"
}
```

The executing agent must determine the exact compatible versions from the repository’s current dependency state and lockfile requirements.

Do not select versions arbitrarily.

Do not upgrade existing dependencies merely to adopt a newer Node.js or pnpm version.

Do not create competing version authorities such as:

- `.nvmrc`;
- `.node-version`;
- a second Node.js version declaration in another repository file;
- a pnpm version that conflicts with `packageManager`;
- a workflow-specific pnpm version that differs from `package.json`.

The GitHub Actions workflow may read the Node.js version from the root `package.json` or use an explicit workflow value derived from it, but the final implementation must preserve **one authoritative repository-owned declaration**.

The workflow must enable and use Corepack so that the exact pnpm version declared in:

```json
{
  "packageManager": "pnpm@<exact-version>"
}
```

is the version used in CI.

Do not install pnpm globally with an unpinned command.

Do not use floating runtime selectors such as:

```yaml
node-version: latest
node-version: node
node-version: lts/*
```

---

## 4.2 Least-Privilege Workflow Authority

The workflow must explicitly restrict its GitHub token permissions.

At the workflow top level, declare:

```yaml
permissions:
  contents: read
```

The CI workflow currently requires repository read access only.

Do not grant:

- `contents: write`;
- `pull-requests: write`;
- `issues: write`;
- `checks: write`;
- `actions: write`;
- `packages: write`;
- deployment permissions;
- release permissions;
- any other write capability.

Do not use `permissions: write-all`.

Do not omit the permissions declaration and rely on repository defaults.

---

## 4.3 Baseline Execution Environment

The workflow must explicitly define:

```yaml
env:
  TZ: UTC
  CI: true
```

These values establish a visible, stable baseline for time-sensitive and CI-sensitive behavior.

This task does **not** establish a global locale policy.

Do not add locale environment variables unless they are required by an existing repository dependency or validation command.

Future tests that depend on locale-specific behavior must define their required locale explicitly rather than relying on the GitHub runner’s default locale.

---

## 4.4 Command Fidelity

The workflow must execute the repository’s canonical validation commands directly.

The required commands are:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

Do not reproduce formatting, linting, TypeScript, or test behavior inside workflow YAML.

Do not replace these commands with workflow-specific alternatives.

Do not add custom wrappers or shell scripts that duplicate repository behavior.

---

## 4.5 Fail-Closed Execution

Each validation gate must be a separate, visible GitHub Actions step.

The required validation order is:

```text
Install dependencies
→ Check formatting
→ Run ESLint
→ Run TypeScript project build
→ Run tests
```

If any command fails, the workflow must fail.

Do not use:

```yaml
continue-on-error: true
```

Do not suppress failures with:

```bash
command || true
```

Do not use shell logic that converts a failed validation command into a successful workflow step.

Tests must not run after a failed TypeScript build.

---

## 4.6 Thin CI Principle

GitHub Actions must orchestrate repository-owned behavior. It must not become an independent build architecture.

The workflow must not contain:

- custom build logic;
- inline application logic;
- custom dependency graph logic;
- duplicate lint configuration;
- duplicate TypeScript configuration;
- duplicate test configuration;
- hidden Bash orchestration;
- workflow-specific validation rules that are absent from the repository.

The repository remains the source of truth. CI executes and enforces it.

---

# 5. Scope

## In Scope

Create one baseline GitHub Actions workflow:

```text
.github/workflows/ci.yml
```

The workflow must:

1. Trigger on:
   - `push`;
   - `pull_request`.
2. Explicitly declare:

```yaml
permissions:
  contents: read
```

1. Explicitly declare:

```yaml
env:
  TZ: UTC
  CI: true
```

1. Use the Node.js version defined by the root repository authority.
2. Enable Corepack.
3. Use the exact pnpm version declared by the root `package.json`.
4. Use the standard pnpm cache integration supported by GitHub Actions tooling.
5. Install dependencies using:

```bash
pnpm install --frozen-lockfile
```

1. Run the repository validation gates in this order:

```bash
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

1. Fail immediately when a validation gate fails.
2. Keep every validation gate visible as a separate workflow step.

---

## Out of Scope

The following are explicitly excluded:

- deployment workflows;
- production deployment;
- release automation;
- package publishing;
- Docker;
- Cloudflare deployment;
- database services;
- secrets;
- environment-specific deployment configuration;
- coverage upload;
- coverage thresholds;
- benchmark execution;
- replay execution;
- conformance execution;
- runtime purity enforcement;
- custom shell orchestration;
- CI matrices;
- operating-system matrices;
- parallel CI jobs;
- application code;
- runtime code;
- API behavior;
- web behavior;
- edge behavior;
- domain behavior;
- registry behavior;
- new workspace `build` scripts;
- dummy or placeholder scripts;
- unrelated dependency upgrades.

Runtime purity enforcement is reserved exclusively for **IT-0108**.

---

# 6. Required Workflow Structure

The workflow must follow this logical sequence:

```text
Workflow trigger
→ Read-only permissions
→ UTC/CI environment baseline
→ Checkout
→ Set up Node.js
→ Enable Corepack
→ Restore standard pnpm cache
→ Install with frozen lockfile
→ Check formatting
→ Run lint
→ Run TypeScript project build
→ Run tests
```

The exact arrangement of Node.js, Corepack, pnpm, and cache setup may follow current GitHub Actions requirements, provided that:

- the toolchain remains explicitly controlled;
- `package.json` remains the authoritative source;
- the exact pnpm version declared in `packageManager` is used;
- no conflicting version source is introduced;
- the cache uses the standard supported mechanism.

---

# 7. Constraints

1. **Root `package.json` is authoritative**

   Node.js must be defined through:

```json
"engines": {
  "node": "<exact-supported-version>"
}
```

pnpm must be defined through:

```json
"packageManager": "pnpm@<exact-version>"
```

1. **Corepack is mandatory**

   CI must enable and use Corepack.

2. **No global floating pnpm installation**

   Do not use:

```bash
npm install -g pnpm
```

Do not use an unpinned pnpm installation command.

1. **Least privilege is mandatory**

   The workflow must explicitly use:

```yaml
permissions:
  contents: read
```

1. **Execution baseline is mandatory**

   The workflow must explicitly use:

```yaml
env:
  TZ: UTC
  CI: true
```

1. **Frozen installation is mandatory**

```bash
pnpm install --frozen-lockfile
```

must be used exactly.

1. **No lockfile mutation**

   CI must not modify `pnpm-lock.yaml`.

2. **No placeholder scripts**

   Do not add workspace `build`, `test`, or other scripts solely to satisfy CI.

3. **No hidden failure handling**

   Do not use `continue-on-error`, `|| true`, or equivalent failure suppression.

4. **No custom workflow orchestration**

Do not add Bash scripts or custom wrappers that duplicate repository validation behavior.

1. **No unrelated changes**

Do not modify application architecture, package boundaries, or production behavior.

---

# 8. Acceptance Criteria

The implementation is accepted only when all of the following are true:

## Workflow

- `.github/workflows/ci.yml` exists.
- The workflow triggers on both `push` and `pull_request`.
- The workflow explicitly declares:

```yaml
permissions:
  contents: read
```

- The workflow explicitly declares:

```yaml
env:
  TZ: UTC
  CI: true
```

- The workflow does not grant write authority.

## Toolchain

- The root `package.json` contains an explicit `engines.node` declaration.
- The root `package.json` contains an exact `packageManager` declaration for pnpm.
- No competing Node.js version authority is introduced.
- No conflicting pnpm version authority is introduced.
- CI enables Corepack.
- CI uses the exact pnpm version declared in `package.json`.
- No floating Node.js selector is used.
- No unpinned global pnpm installation is used.

## Dependency Installation

- CI runs:

```bash
pnpm install --frozen-lockfile
```

- CI does not modify the lockfile.
- The standard pnpm cache mechanism is enabled.

## Validation Gates

The following commands are present as separate visible workflow steps and execute in this order:

```bash
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

- A failed formatting check fails the workflow.
- A failed lint check fails the workflow.
- A failed TypeScript build fails the workflow.
- A failed test run fails the workflow.
- Tests do not execute after a failed TypeScript build.
- No failure suppression is present.

## Architectural Scope

- No dummy or placeholder workspace scripts are added.
- No application code is introduced.
- No runtime, API, web, edge, domain, or registry behavior is introduced.
- No deployment or release behavior is introduced.
- No CI matrix is introduced.
- No unrelated dependency upgrade is introduced.
- Runtime purity enforcement is not added prematurely.

## Local Repository Validation

The following commands must all succeed after implementation:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

The workflow YAML must be syntactically valid.

---

# 9. Required Verification

Run:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

All commands must succeed.

Inspect the final workflow and verify:

- both triggers exist;
- `permissions: contents: read` exists at the workflow top level;
- `TZ: UTC` and `CI: true` exist at the workflow top level;
- the Node.js version is controlled by the root repository authority;
- the pnpm version is controlled by `packageManager`;
- Corepack is enabled;
- the pnpm cache uses the standard supported mechanism;
- installation uses `--frozen-lockfile`;
- validation gates appear in the required order;
- every validation gate is visible as a separate step;
- no failure is suppressed;
- no placeholder workspace scripts were added;
- no unrelated files were modified.

If an existing workflow-validation tool is already available, it may be used.

Do not add a new validation dependency solely for this task.

---

# 10. Definition of Done

AMS-0107 is complete when:

- the GitHub Actions workflow exists and is valid;
- the workflow is explicitly read-only;
- the execution environment explicitly uses UTC and CI mode;
- the root `package.json` is the single repository-owned authority for Node.js and pnpm versions;
- Corepack enforces the exact pnpm version declared by the repository;
- dependency installation is frozen-lockfile based;
- the standard pnpm cache is enabled;
- formatting, linting, TypeScript project references, and tests are enforced in CI;
- all local validation commands pass;
- the workflow remains a thin executor of repository-owned commands;
- no placeholder scripts or unrelated architecture are introduced;
- the implementation is committed;
- the completion report satisfies the reporting requirements below.

---

# 11. Reporting Requirements

The completion report must include:

1. Every file created or modified.
2. The exact Node.js version declared in root `package.json`.
3. The exact pnpm version declared in root `package.json`.
4. Confirmation that Corepack is enabled in CI.
5. Confirmation that `package.json` is the single repository-owned toolchain authority.
6. The exact CI commands and their execution order.
7. Confirmation that the workflow uses:

```yaml
permissions:
  contents: read
```

1. Confirmation that the workflow uses:

```yaml
env:
  TZ: UTC
  CI: true
```

1. The pnpm cache mechanism used.
2. Results of:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

1. Confirmation that no workspace placeholder scripts were added.
2. Confirmation that no deployment, release, or application behavior was introduced.
3. The commit identifier created for the implementation.

---

# 12. Next

After completion and review:

**IT-0108 — Runtime Purity CI Check**

IT-0108 is the constitutional enforcement gate for Runtime purity.

Do not add Runtime purity checks to AMS-0107. This mandate must provide only the CI foundation required for IT-0108 to add that enforcement cleanly.

---

# AMS-0107-R1 — Zero-Trust Remediation of GitHub Actions CI Authority and Cache Configuration

**Status:** Authorized for execution
**Priority:** High — Constitutional foundation correction
**Execution mode:** Zero Trust / Minimal-Diff Remediation
**Supersedes:** The CI implementation portion of AMS-0107 only
**Objective:** Correct the GitHub Actions CI workflow so that Node.js version authority is single-sourced, pnpm authority remains repository-owned, caching is non-overlapping, and the workflow remains a thin, deterministic, least-privilege executor.

---

## 1. Constitutional Objective

The current CI workflow contains two architectural defects that must be corrected before AMS-0107 can be ratified:

1. The Node.js version is duplicated between the root `package.json` and `.github/workflows/ci.yml`, creating split configuration authority.
2. The workflow uses both `actions/setup-node` pnpm caching and a separate manual `actions/cache` step, creating redundant cache management.

This remediation SHALL establish:

- one explicit repository-owned source for the Node.js version;
- one explicit repository-owned source for the pnpm version;
- one cache mechanism only;
- least-privilege workflow permissions;
- deterministic baseline environment settings;
- no hidden CI orchestration;
- no unrelated repository modifications.

The CI workflow SHALL remain a thin executor of repository-defined commands. It SHALL NOT become an independent configuration authority, package manager authority, build system, or architectural policy engine.

---

# 2. Mandatory Zero-Trust Execution Protocol

The executing agent SHALL NOT assume that any previous implementation report is correct.

Before editing any file, the agent MUST independently inspect and record the current contents of:

- `package.json`
- `.github/workflows/ci.yml`
- `pnpm-lock.yaml`
- `.gitignore`
- `.nvmrc`, if it already exists
- any existing Node-version files, including:
  - `.node-version`
  - `.tool-versions`
  - `.node-version`
- any existing GitHub Actions workflows under `.github/workflows/`

The agent MUST also inspect the current versions reported by:

```bash
node --version
pnpm --version
```

The agent SHALL identify every existing declaration of:

- the Node.js version;
- the pnpm version;
- the pnpm cache configuration;
- GitHub Actions permissions;
- workflow-level environment variables.

The agent MUST NOT infer repository state from prior agent reports.

---

# 3. Scope of Authorized Changes

The agent is authorized to modify only the following files unless an additional change is strictly required to preserve repository consistency:

1. `.github/workflows/ci.yml`
2. `.nvmrc`
3. `package.json`

The agent MAY modify `pnpm-lock.yaml` only if a package-manager operation demonstrably changes it. No dependency installation or package upgrade is expected for this remediation.

The agent SHALL NOT modify:

- application code;
- domain code;
- runtime code;
- contracts;
- shared packages;
- edge code;
- test logic;
- Vitest configuration;
- ESLint rules;
- Prettier configuration;
- TypeScript project references;
- workspace package scripts;
- package dependencies;
- package versions;
- constitutional specifications;
- policy documents;
- security documents;
- repository architecture.

The agent SHALL NOT add any new dependency.

The agent SHALL NOT add placeholder scripts.

The agent SHALL NOT create a custom shell script, JavaScript script, or workflow helper solely to resolve version values dynamically.

---

# 4. Toolchain Authority Model

## 4.1 Node.js Authority

Create or update the root `.nvmrc` file.

Its complete content MUST be the exact Node.js version currently declared in:

```json
package.json → engines.node
```

For the current repository state, the expected value is:

```text
20.19.0
```

The `.nvmrc` file MUST contain only the version value, followed by a normal trailing newline.

The workflow MUST consume the Node.js version through `.nvmrc`.

The workflow MUST NOT contain a hardcoded Node.js version.

The workflow SHALL use:

```yaml
with:
  node-version-file: ".nvmrc"
```

The workflow MUST NOT use:

```yaml
node-version: "20.19.0"
```

or any other literal Node.js version.

The root `package.json` `engines.node` value SHALL remain present and unchanged unless inspection proves that its current value differs from the approved repository version.

The agent MUST NOT silently change the Node.js version.

### Required authority relationship

The intended configuration relationship is:

```text
package.json
    └── engines.node
            │
            │ exact value mirrored into
            ▼
.nvmrc
            │
            │ consumed by
            ▼
GitHub Actions setup-node
```

The workflow SHALL consume the repository-owned version file and SHALL NOT independently declare a Node.js version.

---

## 4.2 pnpm Authority

The root `package.json` field:

```json
"packageManager": "pnpm@10.30.3"
```

shall remain the authoritative repository declaration for pnpm.

The agent MUST verify the exact current value before making changes.

The workflow SHALL enable Corepack:

```yaml
- name: Enable Corepack
  run: corepack enable
```

The workflow MUST NOT use:

```bash
corepack prepare pnpm@10.30.3 --activate
```

The workflow MUST NOT hardcode a pnpm version.

The workflow MUST NOT introduce a second pnpm version source.

The workflow SHALL rely on Corepack and the repository’s `packageManager` declaration to select the pnpm version.

The agent MUST verify that the resulting local pnpm execution remains compatible with the lockfile and repository commands.

---

# 5. Cache Authority and Isolation

The workflow currently contains overlapping cache mechanisms.

The agent MUST remove the entire manual `actions/cache` step.

The workflow SHALL use only the standard pnpm cache integration provided by `actions/setup-node`.

The retained Node setup step SHALL include:

```yaml
cache: "pnpm"
cache-dependency-path: "pnpm-lock.yaml"
```

The workflow MUST NOT contain:

- `actions/cache@v4`;
- a manually defined pnpm store path;
- a manually defined cache key;
- `restore-keys`;
- a second pnpm cache mechanism.

The agent MUST NOT add custom cache-key logic.

The final workflow SHALL have exactly one pnpm dependency cache mechanism.

---

# 6. Required Final CI Workflow Properties

The final `.github/workflows/ci.yml` SHALL retain the following properties.

## 6.1 Workflow triggers

The workflow MUST run on:

- `push`
- `pull_request`

The agent SHALL preserve the existing trigger scope unless inspection identifies a syntactic defect.

The agent MUST NOT add:

- scheduled execution;
- manual deployment behavior;
- release triggers;
- workflow dispatch;
- repository dispatch;
- environment promotion triggers.

---

## 6.2 Least-privilege permissions

The workflow MUST explicitly declare:

```yaml
permissions:
  contents: read
```

The workflow MUST NOT receive write authority.

The agent SHALL NOT add:

- `contents: write`;
- `pull-requests: write`;
- `issues: write`;
- `checks: write`;
- `actions: write`;
- `packages: write`;
- `id-token: write`;
- broad write permissions;
- `permissions: write-all`.

The workflow SHALL remain read-only.

---

## 6.3 Deterministic environment baseline

The workflow MUST retain:

```yaml
env:
  TZ: UTC
  CI: true
```

These values MUST remain workflow-level defaults unless a technically necessary reason requires job-level placement.

The agent MUST NOT add locale assumptions or machine-specific environment values.

The agent SHALL NOT claim that these variables make all future tests deterministic. They establish only a controlled baseline for time-zone and CI-mode behavior.

---

## 6.4 Runner

The workflow SHALL continue to use:

```yaml
runs-on: ubuntu-latest
```

No operating-system matrix is authorized.

No Node-version matrix is authorized.

No pnpm-version matrix is authorized.

The agent SHALL NOT add additional jobs.

---

## 6.5 Required command sequence

The workflow SHALL execute the following operations in this order:

1. Checkout repository
2. Set up Node.js using `.nvmrc`
3. Enable Corepack
4. Install dependencies using the frozen lockfile
5. Check formatting
6. Run ESLint
7. Run the TypeScript project-reference build
8. Run the test suite

The required commands are:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

The workflow MUST NOT replace the TypeScript command with:

```bash
pnpm build
```

The workflow MUST NOT use:

```bash
pnpm -r build
```

unless the repository’s authoritative build architecture is separately amended.

The TypeScript project-reference command remains the authoritative CI compilation gate.

---

# 7. Prohibited Behavior

The agent SHALL NOT:

- hardcode the Node.js version in the workflow;
- hardcode the pnpm version in the workflow;
- add a second Node version file;
- add a second pnpm version source;
- use both `setup-node` caching and `actions/cache`;
- add custom cache scripts;
- add dummy workspace build scripts;
- add placeholder package scripts;
- alter package dependencies;
- upgrade Node.js;
- upgrade pnpm;
- regenerate the lockfile without necessity;
- add `continue-on-error`;
- add `|| true`;
- suppress failures;
- add fallback commands;
- add conditional success behavior;
- add deployment or release logic;
- add artifact publishing;
- add coverage upload;
- add third-party CI services;
- add a CI matrix;
- add additional operating systems;
- add additional jobs;
- modify application architecture;
- weaken ESLint boundaries;
- modify Vitest isolation or serial execution;
- modify TypeScript references;
- modify constitutional source documents.

The agent SHALL NOT “clean up” unrelated files.

---

# 8. Required Structural Validation

After making the approved changes, the agent MUST inspect the final files and verify all of the following.

## 8.1 `.nvmrc`

Confirm:

- the file exists at the repository root;
- its version exactly matches `package.json → engines.node`;
- it contains no comments;
- it contains no additional configuration;
- it has a trailing newline.

## 8.2 `package.json`

Confirm:

- `engines.node` remains present;
- the Node.js value was not changed;
- `packageManager` remains present;
- the pnpm value was not changed;
- no scripts were added or altered;
- no dependencies were added, removed, or upgraded.

## 8.3 `.github/workflows/ci.yml`

Confirm:

- `actions/setup-node@v4` uses `node-version-file: ".nvmrc"`;
- no literal Node.js version exists in the workflow;
- `cache: "pnpm"` is present;
- `cache-dependency-path: "pnpm-lock.yaml"` is present;
- `actions/cache` is absent;
- `corepack enable` is present;
- `corepack prepare` is absent;
- no literal pnpm version exists in the workflow;
- `permissions.contents` is `read`;
- `TZ: UTC` is present;
- `CI: true` is present;
- the required validation commands are present in the required order;
- no failure suppression exists;
- no deployment behavior exists;
- no release behavior exists;
- no matrix exists;
- no additional job exists.

---

# 9. Required Local Verification

The agent MUST execute the following commands after the final modifications:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

All commands MUST exit successfully.

The agent SHOULD also run:

```bash
pnpm coverage
```

if the command already exists and does not require unrelated configuration changes.

The agent MUST verify:

```bash
node --version
pnpm --version
```

The reported versions MUST be compatible with:

- `.nvmrc`;
- `package.json → engines.node`;
- `package.json → packageManager`.

The agent MUST NOT change versions merely to make the local environment match.

If the local environment differs from the repository declarations, the agent MUST report the discrepancy rather than silently changing repository authority.

---

# 10. Workflow Validation Limits

The agent MUST distinguish between:

## Local validation

Local execution can prove that the repository commands currently pass:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
```

## GitHub-hosted validation

Local execution cannot prove that GitHub Actions successfully executed the workflow.

The agent MUST NOT state:

> “GitHub Actions CI passed”

unless the workflow was actually committed, pushed, and observed completing successfully in GitHub Actions.

If GitHub-hosted execution is unavailable, the agent MUST state:

> “The workflow was structurally reviewed and the repository gates passed locally. GitHub-hosted execution remains unverified.”

The agent SHALL NOT fabricate:

- a GitHub Actions run ID;
- a workflow URL;
- a commit SHA;
- a successful remote status;
- a remote execution result.

---

# 11. Required Final Diff Audit

Before reporting completion, the agent MUST perform a final repository diff review.

The agent MUST explicitly list:

- every modified file;
- every created file;
- every deleted file.

The agent MUST confirm that the final change set contains only the approved remediation changes.

Expected final scope:

```text
A  .nvmrc
M  .github/workflows/ci.yml
M  package.json
```

If `package.json` already contains the correct authoritative values and does not require modification, it MUST NOT be changed merely to produce an expected diff.

The actual minimal diff is authoritative over the expected example.

No unrelated file may be included.

---

# 12. Definition of Done

AMS-0107-R1 is complete only when all of the following are true:

- The current repository state was independently inspected.
- All Node.js version declarations were identified.
- All pnpm version declarations were identified.
- `.nvmrc` exists and exactly matches `package.json → engines.node`.
- The workflow uses `node-version-file: ".nvmrc"`.
- The workflow contains no hardcoded Node.js version.
- `package.json → packageManager` remains the pnpm authority.
- Corepack is enabled.
- The workflow contains no hardcoded pnpm version.
- The workflow contains no `corepack prepare` command.
- The manual `actions/cache` step has been removed.
- `actions/setup-node` is the sole pnpm cache mechanism.
- `cache: "pnpm"` is configured.
- `cache-dependency-path: "pnpm-lock.yaml"` is configured.
- Workflow permissions remain `contents: read`.
- `TZ: UTC` remains configured.
- `CI: true` remains configured.
- The workflow uses `pnpm install --frozen-lockfile`.
- Formatting runs before linting.
- Linting runs before TypeScript compilation.
- TypeScript compilation runs before tests.
- The authoritative TypeScript command is `pnpm exec tsc -b`.
- No placeholder workspace build scripts were introduced.
- No dependencies were changed.
- No application or architectural code was modified.
- No failure suppression was introduced.
- `pnpm install --frozen-lockfile` passes.
- `pnpm format:check` passes.
- `pnpm lint` passes.
- `pnpm exec tsc -b` passes.
- `pnpm test` passes.
- The final diff contains no unrelated changes.
- The final report distinguishes local verification from GitHub-hosted verification.

---

# 13. Required Final Report Format

The final report MUST use the following structure.

## A. Execution Status

State one of:

- `COMPLETE — locally verified; GitHub-hosted execution not yet verified`
- `COMPLETE — locally and GitHub-hosted verified`
- `BLOCKED — remediation could not be completed`

Do not use ambiguous status language.

## B. Files Changed

For each changed file:

- path;
- created, modified, or deleted;
- concise explanation of the exact change.

## C. Authority Verification

Report:

| Authority | Repository source                          | CI consumption                   | Result    |
| --------- | ------------------------------------------ | -------------------------------- | --------- |
| Node.js   | `package.json → engines.node` and `.nvmrc` | `setup-node → node-version-file` | Pass/Fail |
| pnpm      | `package.json → packageManager`            | Corepack                         | Pass/Fail |

## D. Cache Verification

Explicitly state:

- whether `actions/cache` is absent;
- whether `setup-node` is the only cache mechanism;
- whether `cache-dependency-path` points to `pnpm-lock.yaml`.

## E. Security Verification

Explicitly state:

- workflow permissions;
- whether any write permission exists;
- whether deployment or release authority exists.

## F. Command Results

Report the exit status for:

```text
pnpm install --frozen-lockfile
pnpm format:check
pnpm lint
pnpm exec tsc -b
pnpm test
pnpm coverage (if executed)
```

## G. Final Diff Audit

List all changed files and confirm whether any out-of-scope change was detected.

## H. Remote Verification Status

State exactly whether the workflow was actually executed on GitHub Actions.

Do not claim remote success without direct evidence.

---

# 14. Failure Protocol

If any required verification fails:

1. Stop.
2. Identify the exact failing command or structural requirement.
3. Inspect the relevant file or output.
4. Apply only the smallest justified correction.
5. Re-run the complete required verification sequence.
6. Do not bypass, suppress, or downgrade the failing gate.
7. Do not expand scope without explicit authorization.

If the failure cannot be resolved within the authorized scope, report:

```text
BLOCKED — explicit authorization required
```

and provide:

- the exact failure;
- the affected file;
- the minimal proposed next action;
- why the action exceeds the current mandate.

---

## Final Execution Principle

**Repository authority defines the environment. CI consumes repository authority. CI does not duplicate, reinterpret, or silently override it.**

The executing agent is authorized to proceed only under the constraints above.
